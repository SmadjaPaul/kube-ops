{
  "type": "module",
  "scripts": {
    "build": "cd website && npm run build",
    "install:all": "npm install && cd website && npm install",
    "check:v1-contract": "bash scripts/check-v1-active-contract.sh"
  },
  "devDependencies": {
    "@commitlint/config-conventional": "^21.0.2",
    "@commitlint/cli": "^21.0.2"
  },
  "engines": {
    "node": ">=20.18.1",
    "npm": ">=10.0.0"
  }
}
