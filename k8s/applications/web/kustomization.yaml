apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - project.yaml
  - babybuddy
  - kiwix
  - pinepods
  # V1: business mail stack intentionally disabled; retain manifests for V2.
  # - jmap-webmail
  - trilium
