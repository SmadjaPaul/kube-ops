apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- project.yaml
- openwebui
- karakeep
- litellm
- opencode
- perplexica
- qdrant
# Disabled for initial AOOSTAR bootstrap: GPU/local inference workload
# - vllm-embed
# Disabled for initial bootstrap: optional burst research worker
# - gpt-researcher
- openclaw
# Disabled for initial AOOSTAR bootstrap: local inference workload
# - pocket-tts
