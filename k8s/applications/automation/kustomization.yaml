apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- project.yaml
# Disabled for initial bootstrap: hardware/camera-dependent workload
# - frigate
- mqtt
# Disabled for initial bootstrap: Home Assistant is intentionally deferred
# - hassio
# Disabled for initial bootstrap: hardware-dependent workloads
# - zigbee2mqtt
# - matter-server
