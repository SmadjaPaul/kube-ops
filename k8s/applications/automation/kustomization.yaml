apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- project.yaml
# Disabled for initial bootstrap: hardware/camera-dependent workload
# - frigate
- mqtt
- hassio
# Deferred: upstream coordinator binding 10.0.10.160 is not a Smadja hardware contract.
# - zigbee2mqtt
# Deferred: upstream BLE/LB binding requires local hardware/address allocation.
# - matter-server
