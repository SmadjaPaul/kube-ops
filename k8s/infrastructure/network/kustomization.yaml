apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- project.yaml
- cilium
# Deferred until the Smadja tunnel runtime is bound to the proven local Gateway service.
# - cloudflared
- gateway
- coredns
- policies
